{
  "name": "bugtracker-api-tests",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test:local": "./scripts/run-api-tests.sh --",
    "test:ci": "npx playwright test --config playwright.config.ts $@",
    "test": "echo 'No tests specified'"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.10.10",
    "typescript": "^5.7.3",
    "wait-port": "^1.1.0"
  }
}
