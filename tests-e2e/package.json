{
  "name": "bugtracker-tests",
  "version": "1.0.0",
  "type": "module",
  "directories": {
    "test": "."
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:integration": "./scripts/run-tests.sh --",
    "test:docker": "./scripts/run-docker-tests.sh"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "@types/node": "^22.10.10",
    "typescript": "^5.7.3",
    "wait-port": "^1.1.0"
  }
}
